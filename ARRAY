1.Write a function called middle that takes a list and returns a new list that contains all but the first and last elements.
myList = [1, 2, 3, 4]
middle(myList)  # [2,3]


def middle(lst):
    new = lst[1:]  
    del new[-1]
    return new
    
#another solution 
def middle(lst):
    list =lst
    num = len(lst)
    list.remove(list[0])
    list.remove(list[num-2])
    return list
mylist =[1,2,3,4]
print(middle(mylist))


